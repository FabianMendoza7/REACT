import React from 'react';

// function Header(props){
function Header({titulo}){
// return(<h1 className="encabezado">{props.titulo}</h1>);
return(<h1 className="encabezado">{titulo}</h1>);
}

export default Header;